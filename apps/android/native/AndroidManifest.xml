<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Dioxus exchanges UI updates over an authenticated loopback socket. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <queries>
        <intent><action android:name="android.speech.RecognitionService" /></intent>
    </queries>
    <application android:hasCode="true" android:supportsRtl="true"
        android:icon="@mipmap/ledger_launcher" android:roundIcon="@mipmap/ledger_launcher"
        android:extractNativeLibs="true"
        android:allowBackup="false" android:fullBackupContent="false"
        android:allowNativeHeapPointerTagging="false"
        android:label="Ledgers Bro Test" android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity android:name="dev.dioxus.main.MainActivity" android:exported="true"
            android:configChanges="orientation|screenLayout|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize" android:label="Ledgers Bro Test">
            <meta-data android:name="android.app.lib_name" android:value="dioxusmain" />
            <meta-data android:name="android.app.func_name" android:value="ANativeActivity_onCreate" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
